[project]
name = "spark-sandbox"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "pyspark==3.5.8",
]

[dependency-groups]
test = [
    "pytest>=9.1.0",
]
